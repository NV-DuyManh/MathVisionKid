# Artifacts Workspace

This directory holds final exported models and checkpoints.
Large files should be versioned via DVC and ignored by Git.

- `checkpoints/`: Intermediate training states.
- `exports/`: Final exported models (e.g., ONNX, TorchScript).
- `model-cards/`: Documentation for each model version.
