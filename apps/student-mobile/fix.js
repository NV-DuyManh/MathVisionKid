const fs = require('fs'); const file = 'src/services/analytics/handAiAnalyticsStore.ts'; let content = fs.readFileSync(file, 'utf8'); content = content.replace(/verdict === 'MANUAL_EDIT' \|\| verdict === 'WRONG' \|\| verdict === 'REJECTED' \|\| verdict === 'FAILED' \|\| verdict === 'INCORRECT'\)/g, "verdict === 'MANUAL_EDIT' || verdict === 'WRONG' || verdict === 'REJECTED' || verdict === 'FAILED' || verdict === 'INCORRECT' || verdict === 'AI_CORRECTED' || verdict === 'OCR_CORRECT')"); fs.writeFileSync(file, content);
