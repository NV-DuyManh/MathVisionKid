import { Box, Typography } from '@mui/material';

export default function BatchesPage() {
  return (
    <Box>
      <Typography variant="h5" sx={{ mb: 4 }}>Quản lý Lượt chấm</Typography>
      <Typography color="text.secondary">Danh sách lịch sử các đợt chấm bài sẽ hiển thị ở đây.</Typography>
    </Box>
  );
}
