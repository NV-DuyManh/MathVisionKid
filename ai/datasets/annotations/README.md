# Annotations Workspace

This directory contains annotation schemas and the resulting label files.

- `primary/`: First annotator results.
- `secondary/`: Independent second annotation.
- `adjudicated/`: Resolved gold labels.

Do not overwrite primary labels with secondary labels; preserve disagreement for later analysis.
