# Training Workspace

This directory is intended for ML training configs, scripts, and notebooks.

The Proposal v3.1 specifies the use of **DVC** for dataset and model versioning, and **MLflow** for experiment management.

- Use this workspace for tracking training scripts and configs.
- Models and datasets should be versioned via DVC.
- MLflow can be used to track parameters and metrics (see `experiments/`).
